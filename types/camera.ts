type CameraControlsProps = {
  position: [number, number, number]; // Initial position of the camera
  zoom: number; // Initial zoom level for the orthographic camera
  rotation: [number, number, number]; //  rotation of the camera
};

export type { CameraControlsProps };
